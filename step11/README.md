# Sparse Set
- 順番は保証しない
  - 削除の時速度の速さのため, `O(1)`
  - 一番最後の要素を削除したい要素がある所にコピーする。
  - 一番最後の要素を削除
- Create/Read/Deleteを早くする
- Cacheに親しい
- Dense(Array)とSparse(Map)データ構造を両方使用
- ゲーム業界でよく使われる
- 要素が多い時役に立つけど

## 短所
- **あえて**、、、使う必要があるか
- メモリの使用量が多い
